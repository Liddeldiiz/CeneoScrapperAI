{% extends "layout.html.jinja" %}

{% block middle %}
    <h2 class="title"> Products </h2>
    
    <ul class="productList">
        {% for productBrand in brandList %}
            <li><a href="{{url_for('brands', productBrand=productBrand)}}">{{ productBrand }}</a></li>
        {% endfor %}
    </ul>

{% endblock middle %}